---
import { site } from '../data/site';
import Icon from './Icon.astro';
import LogoWaterAnimation from './LogoWaterAnimation.astro';

const badges = ['Artisan qualifié', 'Intervention locale', 'Devis clair', 'Travail soigné', 'Plomberie & chauffage'];
---
<!-- Bloc plein écran : hero + barre de pastilles = 100vh sous le header -->
<div class="hero-screen">
<section class="hero" id="hero" data-water-scope-start>
  <!-- Fond premium : vidéo cinématique locale + overlays thématiques -->
  <div class="hero__bg" id="hero-bg" aria-hidden="true">
    <video class="hero__video" autoplay muted loop playsinline preload="auto">
      <source src="/assets/cinematique-courtin-bat.mp4" type="video/mp4" />
    </video>
    <div class="hero__overlay"></div>
    <div class="hero__glow hero__glow--1"></div>
    <div class="hero__glow hero__glow--2"></div>
    <div class="hero__glow hero__glow--3"></div>
  </div>

  <div class="container hero__inner">
    <div class="hero__content">
      <div class="reveal-wrap">
        <span class="badge badge--glass hero__tag reveal-item"><Icon name="award" /> Plombier chauffagiste diplômé</span>
      </div>
      
      <h1 class="hero__title">
        <span class="reveal-wrap"><span class="reveal-item block">Courtin&nbsp;Bat,</span></span>
        <span class="reveal-wrap"><span class="reveal-item block text-gradient">artisan d'excellence</span></span>
        <span class="reveal-wrap"><span class="reveal-item block">dans le Vexin & Val-d'Oise.</span></span>
      </h1>
      
      <div class="reveal-wrap">
        <p class="hero__subtitle reveal-item">
          Dépannage, installation, rénovation sanitaire et chauffage avec un travail propre,
          durable et réalisé dans les règles de l’art.
        </p>
      </div>

      <div class="reveal-wrap hero__actions-wrap">
        <div class="hero__actions reveal-item">
          <a href="/contact" class="btn btn--copper btn--lg" data-track="quote_hero">Demander un devis</a>
          <a href={site.phoneHref} class="btn btn--glass btn--lg" data-track="call_hero">
            <Icon name="phone" /> Appeler maintenant
          </a>
        </div>
      </div>

    </div>

    <div class="hero__visual reveal-item-visual">
      <LogoWaterAnimation />
    </div>
  </div>
</section>

<!-- Barre des pastilles sous le hero -->
<section class="badges-banner" aria-label="Atouts de l'entreprise">
  <div class="container badges-banner__inner">
    {badges.map((b) => (
      <span class="badge badge--outline"><Icon name="check" /> {b}</span>
    ))}
  </div>
</section>
</div>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  document.addEventListener("DOMContentLoaded", () => {
    // Parallax background effect
    gsap.to("#hero-bg", {
      yPercent: 30,
      ease: "none",
      scrollTrigger: {
        trigger: "#hero",
        start: "top top",
        end: "bottom top",
        scrub: true
      }
    });

    // Master Timeline for smooth, cinematic entrance
    const tl = gsap.timeline({ defaults: { ease: "power4.out", duration: 0.9 } });

    // Initial state
    gsap.set(".reveal-item", { y: 28, opacity: 0 });
    gsap.set(".reveal-item-visual", { scale: 0.94, opacity: 0, filter: "blur(8px)" });

    tl.to(".reveal-item", {
      y: 0,
      opacity: 1,
      stagger: 0.1,
      delay: 0.1
    })
    .to(".reveal-item-visual", {
      scale: 1,
      opacity: 1,
      filter: "blur(0px)",
      duration: 1.2,
      ease: "power3.out"
    }, "-=0.8");
  });
</script>

<style>
  /* Hero + barre de pastilles remplissent exactement la fenêtre sous le header.
     header (var(--header-h)) + hero-screen (100svh - header) = 100svh */
  .hero-screen {
    display: flex;
    flex-direction: column;
    min-height: calc(100svh - var(--header-h));
  }

  .hero {
    position: relative;
    overflow: hidden;
    background: #0b1118;
    color: #fff;
    /* Plus de margin-top négatif : la vidéo reste alignée SOUS le header,
       elle ne remonte plus derrière la barre de navigation. */
    padding-top: clamp(1.1rem, 4svh, 2.6rem);
    padding-bottom: clamp(1.8rem, 5svh, 3.6rem);
    /* Occupe tout l'espace restant du bloc plein écran */
    flex: 1 1 auto;
    min-height: 0;
    display: flex;
    align-items: center;
    /* Le hero est déjà pleine largeur (bloc enfant de <main>, sans padding).
       On évite 100vw qui, avec une scrollbar classique, déborde de ~18px
       et décale le hero par rapport aux autres sections. */
    width: 100%;
  }
  
  .hero__bg {
    position: absolute;
    inset: -15% 0; /* Parallax vertical uniquement */
    z-index: 0;
    height: 130%;
    width: 100%;
    /* Isole le fond sur sa propre couche GPU : le parallax ne repeint plus la vidéo */
    will-change: transform;
    transform: translateZ(0);
  }

  .hero__video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    background: #000;
    /* Lecture vidéo accélérée par le GPU, sans repaint pendant le scroll */
    transform: translateZ(0);
    backface-visibility: hidden;
  }
  
  .hero__overlay {
    position: absolute;
    inset: 0;
    z-index: 1;
    background:
      linear-gradient(to right, rgba(11, 17, 24, 0.82) 0%, rgba(11, 17, 24, 0.45) 48%, rgba(11, 17, 24, 0.12) 100%),
      linear-gradient(to bottom, rgba(11, 17, 24, 0.5) 0%, rgba(11, 17, 24, 0) 22%, rgba(11, 17, 24, 0) 72%, rgba(11, 17, 24, 0.82) 100%),
      radial-gradient(120% 90% at 50% 35%, transparent 60%, rgba(6, 10, 15, 0.4) 100%);
  }

  .hero__glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.35;
    z-index: 2;
    animation: float 14s ease-in-out infinite;
    will-change: transform;
    transform: translateZ(0);
  }
  .hero__glow--1 {
    width: 500px; height: 500px;
    background: radial-gradient(circle, #4f7da3, transparent 70%);
    top: -10%; right: -10%;
  }
  .hero__glow--2 {
    width: 400px; height: 400px;
    background: radial-gradient(circle, #b8860b, transparent 70%);
    bottom: -10%; left: -10%;
    animation-delay: -7s;
  }
  .hero__glow--3 {
    width: 360px; height: 360px;
    background: radial-gradient(circle, #38bdf8, transparent 70%);
    top: 35%; left: 30%;
    opacity: 0.22;
    animation-delay: -3.5s;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-40px) scale(1.05); }
  }

  .hero__inner {
    position: relative;
    z-index: 10;
    display: grid;
    grid-template-columns: 1fr 1fr; /* Mieux équilibré pour centrer le logo */
    align-items: center;
    gap: clamp(1.5rem, 4vw, 3rem);
    width: 100%;
  }

  .reveal-wrap {
    overflow: hidden;
    padding-block: 0.06rem 0.24rem;
  }
  .block { display: block; }

  .hero__tag { margin-bottom: 0.9rem; }
  
  .hero__title {
    color: #fff;
    margin-bottom: 0.8rem;
    font-size: 3.55rem;
    line-height: 1.06;
    font-weight: 700;
    letter-spacing: 0;
  }
  
  .text-gradient {
    background: linear-gradient(135deg, #fff 0%, #d4af37 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero__subtitle {
    font-size: 1.12rem;
    color: rgba(255, 255, 255, 0.85);
    max-width: 50ch;
    margin-bottom: 1.35rem;
    line-height: 1.45;
    font-weight: 300;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    /* Le margin-bottom a été retiré pour recentrer verticalement le bloc */
  }

  .badge--glass, .btn--glass {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    color: #fff;
    transition: all 0.3s ease;
  }
  .btn--glass:hover {
    background: rgba(255, 255, 255, 0.15);
    transform: translateY(-2px);
    border-color: rgba(255, 255, 255, 0.3);
  }
  .badge--glass {
    padding: 0.5em 1em;
    border-radius: 100px;
  }

  .hero__visual { 
    display: flex; 
    justify-content: center; 
    position: relative;
    filter: drop-shadow(0 20px 40px rgba(0,0,0,0.4));
  }
  .hero__visual :global(.logo-anim) {
    width: min(340px, 34vw);
  }
  
  /* CSS pour la nouvelle barre sous le hero */
  .badges-banner {
    background: var(--surface);
    border-bottom: 1px solid var(--line);
    padding: 0.9rem 0;
  }
  .badges-banner__inner {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 1rem 2.5rem;
  }
  .badge--outline {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.95rem;
    color: var(--ink-2);
    font-weight: 500;
  }
  .badge--outline :global(svg) {
    color: var(--copper);
    width: 18px;
    height: 18px;
  }

  @media (max-width: 1180px) {
    .hero__title { font-size: 3.15rem; }
    .hero__subtitle { font-size: 1.06rem; }
  }

  @media (max-height: 760px) and (min-width: 993px) {
    .hero {
      padding-top: 1rem;
      padding-bottom: 1.6rem;
    }
    .hero__title {
      font-size: 3.05rem;
      margin-bottom: 0.65rem;
    }
    .hero__tag { margin-bottom: 0.65rem; }
    .hero__subtitle {
      font-size: 1.02rem;
      line-height: 1.4;
      margin-bottom: 1rem;
    }
    .hero__visual :global(.logo-anim) {
      width: min(300px, 30vw);
    }
  }

  @media (max-width: 992px) {
    .hero {
      align-items: flex-start;
      padding-top: 1.35rem;
      padding-bottom: 1.8rem;
    }
    .hero__inner { grid-template-columns: 1fr; text-align: center; }
    .hero__title { max-width: none; font-size: 2.7rem; }
    .hero__subtitle { max-width: none; margin-left: auto; margin-right: auto; }
    .hero__actions { justify-content: center; }
    .hero__overlay {
      background:
        linear-gradient(to bottom, rgba(11, 17, 24, 0.55) 0%, rgba(11, 17, 24, 0.35) 45%, rgba(11, 17, 24, 0.88) 100%);
    }
    .hero__visual { display: none; }
  }

  @media (max-width: 768px) {
    .hero__actions-wrap { display: none; }
  }

  @media (max-width: 600px) {
    .hero {
      padding-top: 0.95rem;
      padding-bottom: 1.35rem;
    }
    .hero__tag { margin-bottom: 0.55rem; }
    .hero__title {
      font-size: 2.05rem;
      line-height: 1.08;
      margin-bottom: 0.55rem;
    }
    .hero__subtitle {
      font-size: 1rem;
      line-height: 1.42;
      margin-bottom: 0.85rem;
    }
    .badges-banner__inner { gap: 0.8rem 1.2rem; justify-content: flex-start; }
    /* Boutons empilés et de largeur identique : bords alignés sur mobile */
    .hero__actions {
      flex-direction: column;
      align-items: center;
      width: 100%;
    }
    .hero__actions .btn {
      width: 100%;
      max-width: 340px;
      padding: 0.9rem 1.2rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__glow { animation: none; }
  }
</style>
